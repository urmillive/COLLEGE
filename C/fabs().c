#include<stdio.h>
#include<math.h>

int main()
{
    int a,b;
    a=1234;
    b=-344;

    printf("The absolute value of %d\n",a,fabs(a));
    printf("The absolute value of %d\n",b,fabs(b));
    getch();
    return(0);
}
